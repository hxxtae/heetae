{
  "name": "heetae",
  "description": "hxxtae portfolio website ",
  "version": "1.2.0",
  "scripts": {
    "heroku-prebuild": "npm ci --dev && npm run build",
    "rm_dist": "rm -rf dist",
    "dev": "npm run rm_dist && parcel index.html -d dist",
    "build": "npm run rm_dist && parcel build index.html --public-url ././",
    "sass": "node-sass -wr --source-map true ./src/style/main.scss ./style.css"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hxxtae/heetae.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/hxxtae/heetae/issues"
  },
  "dependencies": {
    "sass-lint": "^1.13.1",
    "update": "^0.7.4"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "concurrently": "^7.0.0",
    "node-sass": "^7.0.1",
    "parcel-bundler": "^1.12.5",
    "sass": "^1.45.2"
  }
}
